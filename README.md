# Knnclassification-on-cancer-dataset
The dataset present in sklearn library.converted the dataset into dataframe of applying the model the dataset was clean
# knn  classifier
1)K-Neighborsneighbors algorithm is a non parametric and lazy learning algorithm - means there is no assumption for underlying data distribution



